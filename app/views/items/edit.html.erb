<%= form_for @item do |f| %>
  <%= f.label :todo, 'Edit to do:'%>
  <%= f.text_field :todo %>
  <%= f.submit "Submit", class: 'btn btn-outline-success' %>
  <%= f.submit "Back", class: 'btn btn-outline-success' %>
<% end %>
